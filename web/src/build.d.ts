// ★ Build marker injected by `define` in `vite.config.ts` (2026-09-16).
// ⚠️ **Shown on screen** (without knowing which build is running, offline issues can't be isolated).
declare const __BUILD_ID__: string
// ★ Build info compared against the distribution origin's RELEASE (`shared/release.ts` / 2026-09-24). ⚠️ null when built without git
declare const __BUILD_INFO__: { commit: string; committedAt: string } | null
